# D6 training toolchain. Pinned from `pip freeze` of the working venv
# ~/edgecdss-train/.venv (Python 3.12.14) on 2026-09-26, restricted to mlx, mlx-lm,
# transformers, tokenizers, huggingface_hub (<2.0) and their direct dependencies.
# Install only with: pip install -r requirements.txt
#
# KNOWN, NOT RESOLVED HERE (see README, "Open"): mlx-lm 0.31.3 declares
# transformers>=5.0.0, and the working venv has transformers 4.57.6 (pinned by
# llama.cpp's converter requirements). pip rejects this set as a fresh install.
filelock==4.0.3
fsspec==2026.9.0
hf-xet==1.6.0
huggingface_hub==0.36.2
Jinja2==3.1.6
mlx==0.32.2
mlx-lm==0.31.3
mlx-metal==0.32.2
numpy==2.2.6
packaging==26.3
protobuf==4.25.9
PyYAML==6.0.3
regex==2026.9.10
requests==2.34.2
safetensors==0.8.0
sentencepiece==0.2.2
tokenizers==0.22.2
tqdm==4.70.1
transformers==4.57.6
typing_extensions==4.16.0
