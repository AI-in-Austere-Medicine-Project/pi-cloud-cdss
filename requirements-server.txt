# EdgeCDSS server-side dependencies (Jetson deploy)
# The repo's requirements.txt is for the voice CLIENT; this is the server set.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
python-dotenv>=1.0.0
openai>=1.30.0
# PINNED to what is deployed and verified. 1.0.0 removed `temperature` from
# messages.create; _chat_anthropic now inspects the installed SDK and omits
# the parameter when it is absent, so BOTH 0.125.x and 1.0.0 work and this pin
# is a statement of what was tested rather than a constraint holding the
# adapter together.
#
# Verified against the live API on 2026-08-22 at this version: claude-haiku-4-5,
# claude-sonnet-5 and claude-opus-5 all round-trip through providers.chat().
anthropic==1.0.0
chromadb>=0.5.0
